- Check if is better keep the CMakeLists and Android.mk into
  Cocos2d_Compile_Helpers on in the project directory.

- Make the scripts accepts other flags like:
    - The android version that is to be compiled.
    - If project is release or debug.

